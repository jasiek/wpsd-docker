#!/command/with-contenv bash
if [ "${WPSD_ENABLE_HOSTAPD:-0}" != "1" ]; then
    echo "hostapd: disabled (WPSD_ENABLE_HOSTAPD != 1)" >&2
    /command/s6-svc -d /run/service/hostapd
    exec /command/s6-pause
fi
exec /usr/sbin/hostapd /etc/hostapd/hostapd.conf
